---
applyTo: "**"
---

The standard convention is that passing tests should be silent - they shouldn't produce log output. This is a fundamental principle of good test design: only failing tests (or tests with explicit verbose flags) should produce output.
